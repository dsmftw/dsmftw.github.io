{
  "name": "dsmftw-portfolio",
  "description": "My personal webiste",
  "version": "1.0.0",
  "author": "Igor Kozlov <ikozlov11@gmail.com>",
  "dependencies": {
    "gatsby": "^1.8.11",
    "gatsby-link": "^1.6.7",
    "gatsby-plugin-react-helmet": "^1.0.2",
    "gatsby-plugin-sass": "^1.0.2"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "main": "n/a",
  "scripts": {
    "deploy": "rm -rf public && gatsby build --prefix-paths && gh-pages -d public",
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --trailing-comma es5 --no-semi --single-quote --write 'src/**/*.js'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gh-pages": "^1.0.0",
    "prettier": "^1.5.3"
  }
}
